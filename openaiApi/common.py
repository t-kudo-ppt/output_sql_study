from openai import OpenAI
from config import openai_api_key

def process_with_openai(model, prompt):
    """
    OpenAI APIを使用して応答を生成する関数。

    Args:
        model (str): 使用するモデル名。
        prompt (list): プロンプトのリスト。各プロンプトは辞書形式で、役割とコンテンツを含む。

    Returns:
        dict: OpenAI APIからのレスポンス。
    """
    # OpenAIクライアントのインスタンスを作成し、APIキーを設定
    # TODO: OPENAI APIを利用するためにインスタンスの宣言、openaiのapiキーの設定を行ってください
    # ただしインスタンス名はclient、openaiのapiキーはopenai_api_keyに設定されています。

    # TODO: OpenAI APIを使用して応答を生成してください
    # response = TODO
    
    return response
